import React from "react";
import { createRoot } from "react-dom/client";
import App2 from "./components/App-video";

const root = createRoot(document.getElementById("root"));

root.render(<App2 />);
